<%= turbo_stream.update 'calendar-days', partial: "#{@rendered_for}_calendar_days", locals: { month: @month, 
                                                                                              service: @service, 
                                                                                              off_days: @off_days, 
                                                                                              service_provider_id: @service.service_provider.id } %>
<%= turbo_stream.update 'calendar-header', partial: 'calendar_header', locals: { month: @month, service: @service, rendered_for: @rendered_for } %>

